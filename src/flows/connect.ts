/* Flow 1 — API Key 검증 (구현: M4) */
import { page } from '../ui/widgets';

export function render(container: HTMLElement): void {
  container.appendChild(page('연결', 'Base URL과 API 키를 검증하고 세션에 저장합니다. (구현 예정 — M4)'));
}
