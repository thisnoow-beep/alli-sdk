/* 목 픽스처 — SSOT §5 명세 기반.
   ⚠️ 응답 스키마가 미문서화된 곳(§9-2 run_conversation 스트림, §9-3 run 형태, 목록 래퍼 키)은
   ASSUMPTION 주석을 달고 Gate G1(실 API 검증)에서 캡처로 교체한다.
   (M2에서 구현) */

export const FIXTURES_TODO = true;
